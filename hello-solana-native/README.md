# hello-solana-native

```shell
cargo new hello_world --lib

# 添加依赖
cargo add solana-program@2.2.12

# 构建程序，生成一个 target/deploy 目录，包含合约和keypair
cargo build-sbf


```

将 edition 字段在 Cargo.toml 中更新为 2021

